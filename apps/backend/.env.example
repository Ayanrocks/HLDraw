PORT=8080
GIN_MODE=debug
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=hldraw_db
# WARNING: DB_SSLMODE=disable is for LOCAL/DEVELOPMENT use only.
# In production, set DB_SSLMODE=require or DB_SSLMODE=verify-full
# and configure proper TLS certificates for your PostgreSQL connection.
DB_SSLMODE=disable
